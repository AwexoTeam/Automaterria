using Automaterria.Code.Resources;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Reflection;
using System.Text;
using System.Threading.Tasks;
using Terraria.ModLoader;

namespace Automaterria.Code
{
    public class FactoryEntry
    {
        //public List<FactoryBase> factories;

        //public void Load(Mod mod)
        //{
        //    factories = typeof(FactoryBase)
        //    .Assembly.GetTypes()
        //    .Where(t => t.IsSubclassOf(typeof(FactoryBase)) && !t.IsAbstract)
        //    .Select(t => (FactoryBase)Activator.CreateInstance(t))
        //    .ToList();

        //    factories.ForEach(x => x.Load(mod));
        //}

        //public void Unload()
        //{
        //    factories.ForEach(x => x.Unload());
        //}
    }
}
